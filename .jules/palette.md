## 2024-05-22 - Icon-Only Buttons
**Learning:** Icon-only buttons (like the GitHub link) are frequently missed in accessibility reviews, leaving screen reader users with no context ("unlabeled button").
**Action:** Always verify icon-only buttons have an `accessibilityLabel` describing the action, not just the icon name.
